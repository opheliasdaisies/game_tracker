%h1 Games

%table
  %tr
    %th Title
    %th
    %th
    %th

    - @games.each do |game|
      %tr
        %td= game.name
        %td= link_to 'View Game', game
        %td= link_to 'Edit Game', edit_game_path(game)
        %td= link_to 'Delete Game', game, :method => 'delete', :data => {:confirm => "Are you sure?"}